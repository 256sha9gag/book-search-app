import { type DefaultTheme } from 'styled-components'

const baseTheme: DefaultTheme = {
  colors: {
    main: 'skyblue',
    secondary: 'gray',
    ligth: 'white',
    dark: 'gray'
  }
}

export default baseTheme
